import pandas

def get_percent_xylem(color, roi):
    roi - read_xls_file
    color -
    total_area = sum(roi$area)
    xlyem_area = sum(area == blue)

    percent_xylem = /sum(area)

def main(experiment_dir):
    for concentation_dir in experiment_dir:
        color_file, roi_file = find_results(concentation_dir)
        get_percent_xylem(color_file, roi_file)
